{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node-terminal",
      "name": "Debugger",
      "env": {
        "ENV": "local"
      },
      "request": "launch",
      "command": "npm run start",
      "cwd": "${workspaceFolder}",
      "skipFiles": [
        "<node_internals>/**"
      ],
    }
  ]
}